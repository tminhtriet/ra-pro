import { Route } from '@angular/router';

import { SearchTicketComponent } from './index';

export const SearchTicketRoutes: Route[] = [
    {
        path: "search-ticket",
        component: SearchTicketComponent
    }
]